# Public URL of the deployed Google Apps Script Web App that receives applications.
# Looks like: https://script.google.com/macros/s/AKfycbx.../exec
# See doc/setup-form-backend.md for how to deploy and obtain it.
# If unset, the Apply form falls back to opening the user's email client (mailto:).
VITE_FORM_ENDPOINT=
